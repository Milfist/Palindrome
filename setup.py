from setuptools import setup

setup(
    name="Palindrome",
    version="1.0.0",
    description="Algorithm to check if a string is a palindrome",
    author="MilfisT",
    url="",
    test_suite="tests",
    packages=["algorithm"]
)
